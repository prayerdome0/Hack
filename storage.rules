rules_version = '2';

// Firebase Storage is intentionally disabled; SIMZ NAXTY media lives in Cloudinary.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
