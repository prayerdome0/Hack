'use client';

import Link from 'next/link';
import { CheckCircle2, Cloud, Database, ExternalLink, KeyRound, ShieldCheck, XCircle } from 'lucide-react';
import { useAuth } from '@/components/providers/auth-provider';
import { AdminGuard, AdminCard, AdminHeading } from '@/components/admin/admin-shared';

export function SettingsPage() { return <AdminGuard><SettingsContent /></AdminGuard>; }
function SettingsContent() {
  const { configured } = useAuth();
  const cloudConfigured = Boolean(process.env.NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME);
  return <div className="space-y-7"><AdminHeading title="Settings" description="Deployment checks and the guardrails behind your music studio." /><div className="grid gap-5 lg:grid-cols-2"><AdminCard className="p-6"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-gold/10 text-gold"><ShieldCheck className="h-5 w-5" /></div><div><h2 className="font-display text-lg font-semibold text-white">Environment health</h2><p className="mt-1 text-xs text-white/35">Safe, server-aware configuration checks.</p></div></div><div className="mt-6 space-y-3"><HealthRow label="Firebase web configuration" ok={configured} detail={configured ? 'Client SDK ready' : 'Add NEXT_PUBLIC_FIREBASE_* variables'} /><HealthRow label="Cloudinary cloud name" ok={cloudConfigured} detail={cloudConfigured ? 'Signed upload client ready' : 'Add NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME'} /><HealthRow label="Server secrets" ok={true} detail="Private values are never rendered in the client" /></div></AdminCard><AdminCard className="p-6"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-white/[.06] text-white/60"><Cloud className="h-5 w-5" /></div><div><h2 className="font-display text-lg font-semibold text-white">Media organization</h2><p className="mt-1 text-xs text-white/35">Signed Cloudinary uploads use predictable folders.</p></div></div><div className="mt-6 space-y-2">{['simz-naxty/audio/', 'simz-naxty/covers/', 'simz-naxty/artists/', 'simz-naxty/playlists/'].map((folder) => <div key={folder} className="flex items-center gap-3 rounded-xl border border-white/[.07] px-3.5 py-3"><Database className="h-4 w-4 text-gold/70" /><code className="text-xs text-white/65">{folder}</code></div>)}</div></AdminCard></div><AdminCard className="p-6"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-white/[.06] text-white/60"><KeyRound className="h-5 w-5" /></div><div><h2 className="font-display text-lg font-semibold text-white">Security notes</h2><p className="mt-1 text-xs text-white/35">Keep these deployment boundaries intact.</p></div></div><ul className="mt-6 grid gap-4 text-sm leading-6 text-white/50 md:grid-cols-3"><li>Cloudinary API secrets stay in <code className="text-gold/80">CLOUDINARY_URL</code> on the server. Only short-lived signatures reach the browser.</li><li>Firestore rules protect favorites, playlists and listening history by Firebase UID.</li><li>Admin API routes verify Firebase ID tokens and the trusted server-side admin profile before writes.</li></ul><Link href="https://firebase.google.com/docs/firestore/security/get-started" target="_blank" className="mt-6 inline-flex items-center gap-2 text-xs font-semibold text-gold hover:text-gold-bright">Review Firestore rules guidance <ExternalLink className="h-3.5 w-3.5" /></Link></AdminCard><p className="text-[11px] leading-5 text-white/25">For production, deploy Firebase rules through your CI/CD pipeline and configure Firebase Admin credentials in the hosting provider&apos;s secret manager.</p></div>;
}
function HealthRow({ label, ok, detail }: { label: string; ok: boolean; detail: string }) { return <div className="flex items-center gap-3 rounded-xl border border-white/[.07] px-3.5 py-3"><span className={ok ? 'text-emerald-300' : 'text-amber-300'}>{ok ? <CheckCircle2 className="h-4 w-4" /> : <XCircle className="h-4 w-4" />}</span><div><p className="text-xs font-medium text-white/75">{label}</p><p className="mt-1 text-[10px] text-white/35">{detail}</p></div></div>; }
